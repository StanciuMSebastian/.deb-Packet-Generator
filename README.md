# .deb-Packet-Generator

This script takes as an argument a directory containing c or c++ files, which must end in .cc, compiles them, archives them and uploades them on a local repository, before
cleaning up.
